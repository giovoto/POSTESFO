# ========== VERCEL / SUPABASE CONFIGURATION ==========

# Conexión a Base de Datos Supabase (Transaction Pooler Recommended)
# Formato: postgres://<user>:<password>@<host>:6543/<db_name>?pgbouncer=true
DATABASE_URL=postgres://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:6543/postgres?pgbouncer=true

# Seguridad (Requerido para generar tokens JWT)
# Usa un string largo y aleatorio. Puedes generar uno con 'openssl rand -base64 32'
JWT_SECRET=tu_secreto_super_seguro_aqui
JWT_EXPIRES_IN=7d

# Entorno (Dejar en production para Vercel)
NODE_ENV=production

# Frontend URL (Para CORS - En vercel se configura automáticamente, pero útil tenerlo explícito)
FRONTEND_URL=https://tu-proyecto.vercel.app

# ========== OPCIONALES (VERCEL BLOB) ==========
# Solo si decides usar almacenamiento de Vercel en lugar de Supabase Storage
# BLOB_READ_WRITE_TOKEN=
